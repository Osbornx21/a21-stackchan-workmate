diff --git a/firmware/CMakeLists.txt b/firmware/CMakeLists.txt
index dbe9da5..ee4d492 100644
--- a/firmware/CMakeLists.txt
+++ b/firmware/CMakeLists.txt
@@ -4,4 +4,5 @@ cmake_minimum_required(VERSION 3.16)

-set(PROJECT_VER "1.4.1")
+set(PROJECT_VER "0.1.0-a21-official-xiaozhi-compatible")
 add_definitions(-DFIRMWARE_VERSION=\"${PROJECT_VER}\")
+add_definitions(-DA21_OEM_AUTOSTART_XIAOZHI=1)

@@ -11,2 +12,2 @@ add_compile_options(-Wno-missing-field-initializers)
 include($ENV{IDF_PATH}/tools/cmake/project.cmake)
-project(stack-chan)
+project(a21-stackchan-official-xiaozhi-compatible)
diff --git a/firmware/main/Kconfig.projbuild b/firmware/main/Kconfig.projbuild
index 304e48d..f8b985e 100644
--- a/firmware/main/Kconfig.projbuild
+++ b/firmware/main/Kconfig.projbuild
@@ -35,2 +35,10 @@ config CUSTOM_ASSETS_FILE

+config A21_STACKCHAN_KEEP_CONTROL_CHANNEL
+    bool "A21 keep quiet control websocket open"
+    depends on BOARD_TYPE_M5STACK_STACK_CHAN
+    default n
+    help
+        Keep a websocket channel open while idle so A21 can keep the device
+        online without starting listening audio.
+
 choice
diff --git a/firmware/main/main.cpp b/firmware/main/main.cpp
index 164a751..c94a8b0 100644
--- a/firmware/main/main.cpp
+++ b/firmware/main/main.cpp
@@ -5,2 +5,4 @@
  */
+#include <audio/audio_codec.h>
+#include <board.h>
 #include <smooth_ui_toolkit.hpp>
@@ -38,2 +40,11 @@ extern "C" void app_main(void)

+#if defined(A21_OEM_AUTOSTART_XIAOZHI) && A21_OEM_AUTOSTART_XIAOZHI
+    auto* codec = Board::GetInstance().GetAudioCodec();
+    if (codec != nullptr) {
+        codec->SetOutputVolume(92);
+    }
+    mclog::tagInfo("main", "A21 starting Xiaozhi mode directly after official apps preload");
+    GetHAL().startXiaozhi();
+    while (1) {
+        GetHAL().feedTheDog();
+        GetHAL().delay(1000);
+    }
+#endif
+
     // Main loop
diff --git a/firmware/sdkconfig.defaults b/firmware/sdkconfig.defaults
index 4db623a..aa977b5 100644
--- a/firmware/sdkconfig.defaults
+++ b/firmware/sdkconfig.defaults
@@ -12,3 +12,16 @@ CONFIG_BOARD_TYPE_M5STACK_STACK_CHAN=y
 CONFIG_SEND_WAKE_WORD_DATA=n
-CONFIG_SR_WN_WN9_HISTACKCHAN_TTS3=y
+CONFIG_OTA_URL="http://47.103.57.217/xiaozhi/ota/"
+CONFIG_USE_HOTSPOT_WIFI_PROVISIONING=y
+# CONFIG_USE_ESP_BLUFI_WIFI_PROVISIONING is not set
+# CONFIG_USE_ACOUSTIC_WIFI_PROVISIONING is not set
+CONFIG_A21_STACKCHAN_KEEP_CONTROL_CHANNEL=y
+# CONFIG_WAKE_WORD_DISABLED is not set
+# CONFIG_USE_ESP_WAKE_WORD is not set
+# CONFIG_USE_AFE_WAKE_WORD is not set
+CONFIG_USE_CUSTOM_WAKE_WORD=y
+CONFIG_CUSTOM_WAKE_WORD="zi yue|zi yue zi yue|ni hao zi yue|xiao zi yue"
+CONFIG_CUSTOM_WAKE_WORD_DISPLAY="紫悦"
+CONFIG_CUSTOM_WAKE_WORD_THRESHOLD=20
+# CONFIG_WAKE_WORD_DETECTION_IN_LISTENING is not set
+# CONFIG_SR_MN_CN_NONE is not set
+CONFIG_SR_MN_CN_MULTINET7_QUANT=y
+# CONFIG_SR_WN_WN9_HISTACKCHAN_TTS3 is not set
 CONFIG_COMPILER_OPTIMIZATION_SIZE=y
diff --git a/firmware/xiaozhi-esp32/main/application.cc b/firmware/xiaozhi-esp32/main/application.cc
index 3b6f07e..396b23a 100644
--- a/firmware/xiaozhi-esp32/main/application.cc
+++ b/firmware/xiaozhi-esp32/main/application.cc
@@ -21,2 +21,6 @@

+static constexpr int64_t A21_VAD_STOP_DEBOUNCE_MS = 520;
+static constexpr int64_t A21_VAD_MIN_LISTENING_MS = 1200;
+static constexpr int64_t A21_NO_SPEECH_LISTENING_TIMEOUT_MS = 7000;
+

@@ -46,2 +50,14 @@ Application::Application() {
     esp_timer_create(&clock_timer_args, &clock_timer_handle_);
+
+    esp_timer_create_args_t vad_stop_timer_args = {
+        .callback = [](void* arg) {
+            Application* app = (Application*)arg;
+            xEventGroupSetBits(app->event_group_, MAIN_EVENT_VAD_STOP_TIMEOUT);
+        },
+        .arg = this,
+        .dispatch_method = ESP_TIMER_TASK,
+        .name = "a21_vad_stop_timer",
+        .skip_unhandled_events = true
+    };
+    esp_timer_create(&vad_stop_timer_args, &vad_stop_timer_handle_);
 }
@@ -53,2 +69,6 @@ Application::~Application() {
     }
+    if (vad_stop_timer_handle_ != nullptr) {
+        esp_timer_stop(vad_stop_timer_handle_);
+        esp_timer_delete(vad_stop_timer_handle_);
+    }
     vEventGroupDelete(event_group_);
@@ -83,2 +103,6 @@ void Application::Initialize() {
     callbacks.on_vad_change = [this](bool speaking) {
+        vad_speaking_ = speaking;
+        if (speaking) {
+            vad_speech_seen_ = true;
+        }
         xEventGroupSetBits(event_group_, MAIN_EVENT_VAD_CHANGE);
@@ -181,3 +205,4 @@ void Application::Run() {
         MAIN_EVENT_ACTIVATION_DONE |
-        MAIN_EVENT_STATE_CHANGED;
+        MAIN_EVENT_STATE_CHANGED |
+        MAIN_EVENT_VAD_STOP_TIMEOUT;

@@ -232,2 +257,3 @@ void Application::Run() {
         if (bits & MAIN_EVENT_VAD_CHANGE) {
+            HandleVadChange(vad_speaking_);
             if (GetDeviceState() == kDeviceStateListening) {
@@ -238,2 +264,6 @@ void Application::Run() {

+        if (bits & MAIN_EVENT_VAD_STOP_TIMEOUT) {
+            HandleVadStopTimeoutEvent();
+        }
+
         if (bits & MAIN_EVENT_SCHEDULE) {
@@ -256,2 +286,7 @@ void Application::Run() {
             }
+#if CONFIG_A21_STACKCHAN_KEEP_CONTROL_CHANNEL
+            if (clock_ticks_ % 10 == 3) {
+                EnsureA21ControlChannel();
+            }
+#endif
         }
@@ -263,2 +298,6 @@ void Application::HandleNetworkConnectedEvent() {
     auto state = GetDeviceState();
+    auto display = Board::GetInstance().GetDisplay();
+    a21_ready_notified_ = false;
+    a21_control_retry_notified_ = false;
+    display->SetChatMessage("system", "正在连接紫悦服务...");

@@ -281,3 +320,2 @@ void Application::HandleNetworkConnectedEvent() {
     // Update the status bar immediately to show the network state
-    auto display = Board::GetInstance().GetDisplay();
     display->UpdateStatusBar(true);
@@ -288,2 +326,4 @@ void Application::HandleNetworkDisconnectedEvent() {
     auto state = GetDeviceState();
+    auto display = Board::GetInstance().GetDisplay();
+    a21_ready_notified_ = false;
+    a21_control_retry_notified_ = false;
     if (state == kDeviceStateConnecting || state == kDeviceStateListening || state == kDeviceStateSpeaking) {
@@ -294,3 +335,3 @@ void Application::HandleNetworkDisconnectedEvent() {
     // Update the status bar immediately to show the network state
-    auto display = Board::GetInstance().GetDisplay();
+    display->SetChatMessage("system", "网络断开，正在重连...");
     display->UpdateStatusBar(true);
@@ -309,3 +348,3 @@ void Application::HandleActivationDoneEvent() {
     display->ShowNotification(message.c_str());
-    display->SetChatMessage("system", "");
+    display->SetChatMessage("system", "正在连接紫悦服务...");

@@ -319,2 +358,5 @@ void Application::HandleActivationDoneEvent() {
         audio_service_.PlaySound(Lang::Sounds::OGG_SUCCESS);
+#if CONFIG_A21_STACKCHAN_KEEP_CONTROL_CHANNEL
+        EnsureA21ControlChannel();
+#endif
     });
@@ -322,2 +364,38 @@ void Application::HandleActivationDoneEvent() {

+void Application::EnsureA21ControlChannel() {
+#if CONFIG_A21_STACKCHAN_KEEP_CONTROL_CHANNEL
+    if (!protocol_) {
+        return;
+    }
+    if (GetDeviceState() != kDeviceStateIdle) {
+        return;
+    }
+    if (protocol_->IsAudioChannelOpened()) {
+        return;
+    }
+
+    auto display = Board::GetInstance().GetDisplay();
+    if (!a21_ready_notified_) {
+        display->SetChatMessage("system", "正在连接紫悦服务...");
+    }
+    ESP_LOGI(TAG, "A21 keeping quiet control websocket open");
+    if (!protocol_->OpenAudioChannel()) {
+        ESP_LOGW(TAG, "A21 quiet control websocket open failed");
+        if (!a21_control_retry_notified_) {
+            display->ShowNotification("紫悦服务连接中，请稍等", 30000);
+            display->SetChatMessage("system", "正在连接紫悦服务...");
+            a21_control_retry_notified_ = true;
+        }
+        return;
+    }
+    a21_control_retry_notified_ = false;
+    if (!a21_ready_notified_) {
+        display->ShowNotification("紫悦已就绪", 5000);
+        display->SetChatMessage("system", "紫悦已就绪，可以叫我");
+        a21_ready_notified_ = true;
+    }
+    SetDeviceState(kDeviceStateIdle);
+#endif
+}
+
 void Application::ActivationTask() {
@@ -780,2 +858,6 @@ void Application::HandleWakeWordDetectedEvent() {
     if (!protocol_) {
+        auto display = Board::GetInstance().GetDisplay();
+        display->ShowNotification("紫悦正在启动，稍等一下", 3000);
+        display->SetChatMessage("system", "正在连接紫悦服务...");
+        audio_service_.EnableWakeWordDetection(true);
         return;
@@ -833,2 +915,5 @@ void Application::ContinueWakeWordInvoke(const std::string& wake_word) {
             audio_service_.EnableWakeWordDetection(true);
+            auto display = Board::GetInstance().GetDisplay();
+            display->ShowNotification("紫悦服务连接失败，正在重试", 5000);
+            display->SetChatMessage("system", "正在连接紫悦服务...");
             return;
@@ -828,16 +913,17 @@ void Application::HandleStartListeningEvent() {
         if (!protocol_->IsAudioChannelOpened()) {
             SetDeviceState(kDeviceStateConnecting);
             // Schedule to let the state change be processed first (UI update)
-            Schedule([this]() {
-                ContinueOpenAudioChannel(kListeningModeManualStop);
+            ListeningMode mode = GetDefaultListeningMode();
+            Schedule([this, mode]() {
+                ContinueOpenAudioChannel(mode);
             });
             return;
         }
-        SetListeningMode(kListeningModeManualStop);
+        SetListeningMode(GetDefaultListeningMode());
     } else if (state == kDeviceStateSpeaking) {
         AbortSpeaking(kAbortReasonNone);
-        SetListeningMode(kListeningModeManualStop);
+        SetListeningMode(GetDefaultListeningMode());
     }
 }
@@ -872,2 +957,7 @@ void Application::HandleStateChangedEvent() {
             display->SetEmotion("neutral"); // Then set emotion (wechat mode checks child count)
+            if (vad_stop_timer_handle_ != nullptr) {
+                esp_timer_stop(vad_stop_timer_handle_);
+            }
+            vad_speaking_ = false;
+            vad_speech_seen_ = false;
             audio_service_.EnableVoiceProcessing(false);
@@ -913,2 +1003,7 @@ void Application::HandleStateChangedEvent() {
             display->SetStatus(Lang::Strings::SPEAKING);
+            if (vad_stop_timer_handle_ != nullptr) {
+                esp_timer_stop(vad_stop_timer_handle_);
+            }
+            vad_speaking_ = false;
+            vad_speech_seen_ = false;

@@ -947,4 +1042,64 @@ void Application::AbortSpeaking(AbortReason reason) {

+void Application::HandleVadChange(bool speaking) {
+    if (GetDeviceState() != kDeviceStateListening) {
+        return;
+    }
+
+    if (speaking) {
+        vad_speech_seen_ = true;
+        if (vad_stop_timer_handle_ != nullptr) {
+            esp_timer_stop(vad_stop_timer_handle_);
+        }
+        return;
+    }
+
+    if (listening_mode_ != kListeningModeAutoStop) {
+        return;
+    }
+
+    if (!vad_speech_seen_ || vad_stop_timer_handle_ == nullptr) {
+        return;
+    }
+
+    int64_t elapsed_ms = (esp_timer_get_time() - listening_started_at_us_) / 1000;
+    int64_t delay_ms = A21_VAD_STOP_DEBOUNCE_MS;
+    if (elapsed_ms < A21_VAD_MIN_LISTENING_MS) {
+        delay_ms += A21_VAD_MIN_LISTENING_MS - elapsed_ms;
+    }
+    esp_timer_stop(vad_stop_timer_handle_);
+    esp_timer_start_once(vad_stop_timer_handle_, delay_ms * 1000);
+    ESP_LOGI(TAG, "A21 device VAD silence detected, stop in %lld ms", (long long)delay_ms);
+}
+
+void Application::HandleVadStopTimeoutEvent() {
+    if (GetDeviceState() != kDeviceStateListening || vad_speaking_) {
+        return;
+    }
+
+    int64_t elapsed_ms = (esp_timer_get_time() - listening_started_at_us_) / 1000;
+    if (!vad_speech_seen_) {
+        ESP_LOGI(TAG, "A21 no-speech timeout, stop listening elapsed=%lld ms", (long long)elapsed_ms);
+        StopListening();
+        return;
+    }
+
+    if (listening_mode_ != kListeningModeAutoStop) {
+        return;
+    }
+
+    if (elapsed_ms < A21_VAD_MIN_LISTENING_MS && vad_stop_timer_handle_ != nullptr) {
+        int64_t delay_ms = A21_VAD_MIN_LISTENING_MS - elapsed_ms + A21_VAD_STOP_DEBOUNCE_MS;
+        esp_timer_start_once(vad_stop_timer_handle_, delay_ms * 1000);
+        return;
+    }
+
+    ESP_LOGI(TAG, "A21 device VAD stop listening elapsed=%lld ms", (long long)elapsed_ms);
+    StopListening();
+}
+
 void Application::SetListeningMode(ListeningMode mode) {
     listening_mode_ = mode;
+    listening_started_at_us_ = esp_timer_get_time();
+    vad_speaking_ = false;
+    vad_speech_seen_ = false;
+    if (vad_stop_timer_handle_ != nullptr) {
+        esp_timer_stop(vad_stop_timer_handle_);
+    }
+    if (mode != kListeningModeRealtime && vad_stop_timer_handle_ != nullptr) {
+        esp_timer_start_once(vad_stop_timer_handle_, A21_NO_SPEECH_LISTENING_TIMEOUT_MS * 1000);
+    }
     SetDeviceState(kDeviceStateListening);
diff --git a/firmware/xiaozhi-esp32/main/audio/wake_words/custom_wake_word.cc b/firmware/xiaozhi-esp32/main/audio/wake_words/custom_wake_word.cc
index 5ca6a37..fe49f98 100644
--- a/firmware/xiaozhi-esp32/main/audio/wake_words/custom_wake_word.cc
+++ b/firmware/xiaozhi-esp32/main/audio/wake_words/custom_wake_word.cc
@@ -91,3 +91,21 @@ bool CustomWakeWord::Initialize(AudioCodec* codec, srmodel_list_t* models_list)
 #ifdef CONFIG_CUSTOM_WAKE_WORD
         threshold_ = CONFIG_CUSTOM_WAKE_WORD_THRESHOLD / 100.0f;
-        commands_.push_back({CONFIG_CUSTOM_WAKE_WORD, CONFIG_CUSTOM_WAKE_WORD_DISPLAY, "wake"});
+        std::string command_list = CONFIG_CUSTOM_WAKE_WORD;
+        size_t start = 0;
+        int command_count = 0;
+        while (start <= command_list.size()) {
+            size_t end = command_list.find('|', start);
+            std::string command = command_list.substr(start,
+                end == std::string::npos ? std::string::npos : end - start);
+            size_t first = command.find_first_not_of(" \t\r\n");
+            size_t last = command.find_last_not_of(" \t\r\n");
+            if (first != std::string::npos && last != std::string::npos) {
+                command = command.substr(first, last - first + 1);
+                commands_.push_back({command, CONFIG_CUSTOM_WAKE_WORD_DISPLAY, "wake"});
+                command_count++;
+            }
+            if (end == std::string::npos) {
+                break;
+            }
+            start = end + 1;
+        }
+        ESP_LOGI(TAG, "Loaded %d A21 custom wake command(s) for %s", command_count, CONFIG_CUSTOM_WAKE_WORD_DISPLAY);
 #endif
     } else {
diff --git a/firmware/xiaozhi-esp32/main/application.h b/firmware/xiaozhi-esp32/main/application.h
index 7ca7af4..46cfdac 100644
--- a/firmware/xiaozhi-esp32/main/application.h
+++ b/firmware/xiaozhi-esp32/main/application.h
@@ -33,2 +33,3 @@
 #define MAIN_EVENT_STATE_CHANGED        (1 << 12)
+#define MAIN_EVENT_VAD_STOP_TIMEOUT     (1 << 13)

@@ -131,2 +132,3 @@ private:
     esp_timer_handle_t clock_timer_handle_ = nullptr;
+    esp_timer_handle_t vad_stop_timer_handle_ = nullptr;
     DeviceStateMachine state_machine_;
@@ -142,2 +144,7 @@ private:
     bool play_popup_on_listening_ = false;  // Flag to play popup sound after state changes to listening
+    bool vad_speaking_ = false;
+    bool vad_speech_seen_ = false;
+    bool a21_ready_notified_ = false;
+    bool a21_control_retry_notified_ = false;
+    int64_t listening_started_at_us_ = 0;
     int clock_ticks_ = 0;
@@ -155,5 +162,8 @@ private:
     void HandleWakeWordDetectedEvent();
+    void HandleVadChange(bool speaking);
+    void HandleVadStopTimeoutEvent();
     void ContinueOpenAudioChannel(ListeningMode mode);
     void ContinueWakeWordInvoke(const std::string& wake_word);
+    void EnsureA21ControlChannel();

     // Activation task (runs in background)
